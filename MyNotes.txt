# Server Travelling
PuzzlePlatformsGameInstance
- Host()
* Get UWorld
* ensure null check UWorld pointer
* Call ServerTravel with an url -> "/Game/ThirdPersonCPP/Maps/ThirdPersonExampleMap" It needs where's the map.
* Add ?listen to the url -> "/Game/ThirdPersonCPP/Maps/ThirdPersonExampleMap?listen"


# Open a new instance of the game in standalone
* Locate the uproject.
* Add the ip and -game -log parameters
Ex: 192.168.1.90 -game -log
* It will connect to a listen server in the editor.
There's an error the loaded player is not visible. Due the unsaved map.
Solution: Save the map to match both standalone and editor settings.


# Console Commands With Exec
- Implement cheats and fast commands
- Escape key
UEngine* Engine = GetEngine();
if(!ensure) -> Check if Engine is not nullptr.
For printing on screen: Engine->AddOnScreenDebugMessage().
UFUNCTION(Exec) over the function to make the console command.


# Client Travel
Operates on a player controller level. Connects to a server
Join function
* APlayerController instance -> GetFirstLocalPlayerController();
* Nullcheck using ensure.
* PlayerController->ClientTravel(Addressm ETravelType::TRAVEL_Absolute);


# Sharing the game on Itch.io
* Building the game.
Package project and select the platform.
Have an account.
Upload it.


==================================================================
# Advanced UMG Widget Layout #
Containers -> Vertical and Horizontal box, layout.
Size boxes -> Child layout: reposition certain size and aspect ratio.
Overlay -> Which things overlays other in the group.


==================================================================
UEditableTextBox => Add it to the reference.
BindWidget and change the name.
Bind the second join button.
Set the join as an interface function. Remember = 0 is pure virtual.

JoinServer
const FString& Address = IPAddressField->GetText().ToString();
Passed to Join.


==================================================================
Enable the STEAM plugin
Online Subsystem Steam -> Enable it
.Build.cs
OnlineSubsystemSteam

DefaultEngine.ini
= From NULL to Steam
Copy and paste what it's needed.

480 is the APP ID free that can be used. Have to pay for the right one
https://dev.epicgames.com/documentation/en-us/unreal-engine/online-subsystem-steam-interface-in-unreal-engine?application_version=5.6

Have to check outside of the editor.
Powershell or command prompt

Steam running and logged in
#On MAC it's a different issue#
* Build the project.
* And add a non steam game.
* Add the built game.
* For checking the log, look for the console.

#ON PC#
On the commandline:
*Find the path to t he .uproject
-> -game -log
On the build, press key and set 
Log LogOnline VeryVerbose


= NO STEAM TEST
-game -log -nosteam


==================================================================
Layout the text
Clipping on the text: - Set it to clip to bounds
Have a BP header boolean to check if the row instance ios header


==================================================================
Custom Session settings
(FOnlineSessionSettings)
---> On Init
SessionSettings.Set(TEXT("Test"), FString("Hello"), EOnlineDataAdvertisementType::ViaOnlineServiceAndPing);

---> On the callback OnFindSessionsComplete
FString Data;
if(SearchResult.Session.SessionSettings.Get(TEXT("Test"), Data))
{
    UE_LOG(LogTemp, Warning, TEXT("Data found in settings %s"), *Data); 
}
else
{
    UE_LOG(LogTemp, Warning, TEXT("Didn't get expected data")); 
}

---> Editor side
* Model the UI from joining the session.
* Making work for the hosted session.
* Grab the same order on the main menu and create the host menu

---> Interface 
Host(FString ServerName)
Set the new function in all the missing places

---> PuzzlePlatformsGameInstance
Declare FString DesiredServerName; 
And change all the placeholder FString with the DesiredServerName

Create a const key and pass it to all the set and get of the SessionSettings

if(SearchResult.Session.SessionSettings.Get(SERVER_NAME_SETTINGS_KEY, Data))
{
    Data.Name = ServerName
}
else
{
    Data.Name = "Could not find server name";
}

* Bind the host menu on MainMenu
* Bind text box and Buttons  
 
New functions
- OpenHostMenu()

void UMainMenu::OpenHostMenu()
{
    // MenuSwitcher to the host menu
}

Declare a BindWidget
UEditableTextBox* ServerHostName;
UButton* ConfirmHostMenuButton;
UButton* CancelHostMenuButton;

// Add buttons callback
CancelHostMenuButton same as OpenMainMenu
ConfirmHostMenuButton -> HostServer -> Get the ServerHostName text and we pass it to the MenuInterface->Host(ServerName);


==================================================================
GameMode and Lobby
-> GameMode
It's only on the server.

-> GameInstance
Check where's the spawning done.
Instead of traveling to the map, select the lobby.

-> Game class for the lobby
Create a class calle LobbyGameMode based on the custom game mode.
Custom Game Mode has a constructor defined and initializing the default pawn class.
Ex:
APuzzlePLatformsGameMode::APuzzlePLatformsGameMode()
{
    static CosntructorHelpers::FClassFinder<APawn> PlayerPawnBPClass(TEXT("pawn-route"));
    if(PlayerPawnBPClass.Class != null)
    {
        DefaultPawnClass = PlayerPawnBPClass.Class;
    }
}

LobbyGameMode.h
-> override the PostLogin function and Logout

public:
    virtual void PostLogin(APlayerController* NewPlayer) override;
    virtual void Logout(AController* Exiting) override;

private:
    uint32 NumberOfPlayers = 0;

LobbyGameMode.cpp

void ALobbyGameMode::PostLogin(APlayerController* NewPlayer)
{
    Super::PostLogin(NewPlayer);
    NumberOfPlayers++;

    if(NumberOfPlayers >= 3)
    {
        UWorld* World = GetWorld();
        if(ensure(World != nullptr)) return;

        bUseSeamlessTravel = true;
        World->ServerTravel("Destination_Path?listen");
    }
}

void ALobbyGameMode::Logout(AController* Exiting) 
{
    Super::Logout(Exiting);
    NumberOfPlayers--;
}


==================================================================
NOTE:
Non-seamless
-> Joining a map to disconnect and connect.

Seamless
-> Moving everybody to a transition map.
-> Have a map loaded. 
-> Transition first to a small map. Like a loading screen.
-> Both maps loaded.
By Default there's a transition map.

# Adding a transition map #
Create a Widget loading and add some text.
Seamless is not supported in editor, have to run game instances. (UE4) Don't know if that would be the case for UE5


==================================================================
LobbyGameMode
-> StartGame
-> All the post login to check and do the travel.

Create a timer to give a wait time.
-> Header file
    -> FTimerHandle GameStartTimer

GetWorldTimerManager().SetTimer(GameStartTimer, this, &ALobbyGameMode::StartGame, 5);

void ALobbyGameMode::StartGame()
{
    auto GameInstance = Cast<PuzzlePlatformsGameInstance>(GetGameInstance());
    if(GameInstance == nullptr) return;

    GameInstance->StartSession();

    UWorld* World = GetWorld();
    if(!ensure(World != nullptr)) return;

    bUseSeamlessTravel = true;
    World->ServerTravel("");
}


PuzzlePlatformsGameInstance
-> Function StartSession

void PuzzlePlatformsGameInstance::StartSession()
{
    if(SessionInterface.IsValid())
    {
        SessionInterface->StartSession(SESSION_NAME);
    }
}